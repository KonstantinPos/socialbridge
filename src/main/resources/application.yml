spring:
  application:
    name: post-relay

server:
  port: 8081

telegram:
  bot-token:
  source-chat-ids:
  polling-interval-ms: 30000
  timeout-sec: 25
  base-url: "https://api.telegram.org"

vk:
  user-access-token:
  group-id:
  api-version: "5.199"
  base-url: "https://api.vk.com/method"

logging:
  level:
    com:
      posysaev:
        socialbridge: DEBUG